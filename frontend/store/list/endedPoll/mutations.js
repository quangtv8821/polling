export default {
    ADD_POLLS(state, polls) {
        state.polls = polls
    },
    // ADD_MOST_VOTE(state, data) {
    //     for(let i = 0; i < state.polls.length; i++) {
    //         state.polls[i].most_vote = data
    //     }
    // }
}