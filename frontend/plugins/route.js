export default ({ app }) => {
    // Every time the route changes (fired on initialization too)
    // app.router.beforeEach((to, from, next) => {
    //     const loggedIn = window.localStorage.getItem('user_token')
      
    //     if (!loggedIn) {
    //       return next('/login');
    //     }
    // })
 }